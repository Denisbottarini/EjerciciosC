/*
============================================================================
 Name        : Clase14-09Programacion.c
 Author      : Denis Bottarini
 Version     :
 Copyright   : Your copyright notice
 Description : Hello World in C, Ansi-style
============================================================================
*/

#include <stdio.h>
#include <stdlib.h>

int main(void) {
	setbuf(stdout, NULL);




	return 0;
}
/*

fget= es como el scanf mas seguro con 3 parametros, es para caracteres

strlen= te da lo que efectivamente tiene escrito

reemplazamos el "\n" con "\0" para dar finalizacion a nuestra cadena de caracteres

atoi= convierte a enteros

atof= convierte a flotantes

el factorial es multiplicar ese numero por sus antecesores osea 5*4*3*2*1

recursividad = es una funcion que se llama asi misma

es mas eficiente hacer un for que una funcion recursiva

matrices = es igual que un array el primero seria la columna, (cuantos nombres)
y el segundo las filas (cuantas letras guardo), se representa igual que un array pero doble

que es el nombre del array?= es la direccion de memoria de donde comienza osea del indice cero (0)

cuando paso un array de 2 dimensiones, tengo que pasarle el segundo valor el de la dimension de cuantas letras guardo osea las filas

buscar strcpy para que es

continuo = corta el ciclo donde estas pero hace la otra vuelta

harcodear = es por ejemplo dar valores a un array a cada indice {35, 26, 39, 46}

metodo de ordenamiento por burbujeo = va comparando numeros de a pares y se va a ir pregunrtando

swap = es un cambio de lugar de dos numeros cuando los estamos ordenando por ejemplo dentro de un array anidado o matriz

siempre que itero por un array y necesito iterarlo necesito darle a mi funcion la LONGITUD siempre!! ejemplo (int len)

siempre valido que no sea NULL ejemplo "if(pArray != NULL && len >0){}"

variable para dar el uso del SWAP int flagswap;

buffer = me sirve como anotador para guardar variables momentaneamente

en el retorno siempre por defecto decimos que esta todo mal

el flag cambia cuando su valor cambia

stryncmpy = para comparar los styng tengo que usar el




*/
